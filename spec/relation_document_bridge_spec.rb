RSpec.describe RelationDocumentBridge do
  it "has a version number" do
    expect(RelationDocumentBridge::VERSION).not_to be nil
  end

  it "does something useful" do
    byebug
    expect(false).to eq(true)
  end
end
